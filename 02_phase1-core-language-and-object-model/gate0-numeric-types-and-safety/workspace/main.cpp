#include <iostream>

// Gate 0 starter: keep the numeric types explicit and use brace initialization
// for each required variable before printing the report.
int main() {
    // Finish the task here.
    return 0;
}
