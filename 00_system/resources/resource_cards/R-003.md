# R-003 Card

## Resource
- ID: `R-003`
- Title: cppreference.com (C++ language and standard library reference)
- Role in repo: Precise follow-up definitions after the learner already has a first mental model
- Best use: Clarifying lifetime or destructor wording for `Gate 1: Stack, Heap, and Storage Basics` and `Gate 3: First Class, Lifetime, and Basic RAII`

## Gate 1 Packet: Stack, Heap, and Storage Basics
Use this packet only when the learner already has a basic explanation and still needs a precise definition.

Read only the needed part:

| Need to understand | Read this | Direct link | Stop when you can explain |
|--------------------|-----------|-------------|----------------------------|
| automatic, static, and dynamic storage duration | Storage duration | https://en.cppreference.com/w/cpp/language/storage_duration | what "storage duration" means and which category each object in the gate belongs to |
| object lifetime as a runtime property | Lifetime | https://en.cppreference.com/w/cpp/language/lifetime.html | why an object can stop existing even if a pointer or name still exists |

## Scope Control for Gate 1
This reference is not the first explanation for `Gate 1: Stack, Heap, and Storage Basics`.

Use it:
- to tighten terminology after `R-001` is not enough
- to confirm a precise definition of storage duration or lifetime

Do not use it as the learner's first reading if `R-001` is enough.

## Gate 3 Packet: First Class, Lifetime, and Basic RAII
Use this packet only when the learner already has a basic explanation and still needs a precise definition.

Read only the needed part:

| Need to understand | Read this | Direct link | Stop when you can explain |
|--------------------|-----------|-------------|----------------------------|
| when an object's lifetime begins and ends in a scope-based program | Lifetime | https://en.cppreference.com/w/cpp/language/lifetime.html | why the inner object is destroyed before the outer object in this gate |
| what a destructor is and when it is invoked | Destructor | https://en.cppreference.com/w/cpp/language/destructor | why cleanup code in a destructor runs automatically when the object is destroyed |

## Scope Control for Gate 3
This reference is not the first explanation for `Gate 3: First Class, Lifetime, and Basic RAII`.

Use it:
- to tighten terminology after `R-001` is not enough
- to confirm that destruction happens automatically when the object's lifetime ends

Do not use it as the learner's first reading if `R-001` is enough.

## Gate 4 Packet: Phase 0 Integration Project
Use this packet only when the learner already has a basic integration plan and still needs a precise definition.

Read only the needed part:

| Need to understand | Read this | Direct link | Stop when you can explain |
|--------------------|-----------|-------------|----------------------------|
| which object category is global, local, or dynamic in the integration task | Storage duration | https://en.cppreference.com/w/cpp/language/storage_duration | how to name the storage category of the key variables in the project |
| when the inner and outer objects stop existing | Lifetime | https://en.cppreference.com/w/cpp/language/lifetime.html | why the inner scope object is destroyed before the outer one |
| what the destructor guarantees in the project | Destructor | https://en.cppreference.com/w/cpp/language/destructor | why destructor-based cleanup is deterministic in the integration gate |

## Scope Control for Gate 4
This reference is not the first explanation for `Gate 4: Phase 0 Integration Project`.

Use it:
- to tighten terminology after `R-001` is not enough
- to confirm exact lifetime, storage-duration, or destructor wording during repetition

Do not use it as the learner's first reading if `R-001` is enough.
