# R-001 Card

## Resource
- ID: `R-001`
- Title: LearnCpp.com - "Learn C++" tutorials
- Role in repo: Primary reading path for early phases, especially `Phase 0: C++ Foundations` and narrowed `Phase 1` rollout
- Best use: First explanation whenever the planned concept is covered well enough for learner-facing use

## Gate 0 Packet: Compile, Run, and Basic I/O
Use this packet for:
- source file and compile flow
- basic `std::cout` / `std::cin`
- warnings as part of the task

Read in this order:

| Step | Read this | Direct link | Why this step matters | Stop when you can explain |
|------|-----------|-------------|------------------------|----------------------------|
| 1 | Introduction to the compiler, linker, and libraries | https://www.learncpp.com/cpp-tutorial/introduction-to-the-compiler-linker-and-libraries/ | Gives the basic mental model of what happens before a program runs | what the compiler does, what the linker does, and why an executable exists at the end |
| 2 | Compiling your first program | https://www.learncpp.com/cpp-tutorial/compiling-your-first-program/ | Connects source file, compile action, and running the built program | what gets compiled and what gets run |
| 3 | Configuring your compiler: Warning and error levels | https://www.learncpp.com/cpp-tutorial/configuring-your-compiler-warning-and-error-levels/ | Explains why warnings are part of the work and not optional cleanup | why warnings matter even when the program still builds |
| 4 | Introduction to iostream: cout, cin, and endl | https://www.learncpp.com/cpp-tutorial/introduction-to-iostream-cout-cin-and-endl/ | Covers the exact I/O model needed for `Gate 0: Compile, Run, and Basic I/O` | how input reaches the program and how output is printed |

## Scope Control for Gate 0
For `Gate 0: Compile, Run, and Basic I/O`, do not keep reading forward just because the site continues.

You are ready to stop when you can explain:
- what `main.cpp` is
- what `g++` is doing at a high level
- why warnings matter
- how `std::cin` and `std::cout` are used in a two-number sum program

## Gate 1 Packet: Stack, Heap, and Storage Basics
Use this packet for:
- block scope and automatic lifetime
- static storage duration through a global variable
- dynamic allocation with `new` and `delete`
- the difference between the local pointer variable and the dynamically allocated object

Read in this order:

| Step | Read this | Direct link | Why this step matters | Stop when you can explain |
|------|-----------|-------------|------------------------|----------------------------|
| 1 | Local variables | https://www.learncpp.com/cpp-tutorial/local-variables/ | Gives the basic model for block-local variables and automatic storage duration | what happens to a local variable when its block ends |
| 2 | Dynamic memory allocation with new and delete | https://www.learncpp.com/cpp-tutorial/dynamic-memory-allocation-with-new-and-delete/ | Explains what `new` creates, what `delete` does, and why leaks happen | why a pointer variable and the dynamic object it points to are not the same thing |
| 3 | The stack and the heap | https://www.learncpp.com/cpp-tutorial/the-stack-and-the-heap/ | Connects the duration model to the common stack/heap vocabulary used in this gate | why "stack vs heap" is only a shorthand for the underlying lifetime/storage model |
| 4 | Scope, duration, and linkage summary | https://www.learncpp.com/cpp-tutorial/scope-duration-and-linkage-summary/ | Optional follow-up only when the names `automatic`, `static`, and `dynamic` are still unclear after steps 1-3 | which category name fits each object in the gate |

## Scope Control for Gate 1
For `Gate 1: Stack, Heap, and Storage Basics`, do not keep reading into broader pointer material yet.

Treat step 4 as a fallback clarification, not as the main learning path.

You are ready to stop when you can explain:
- why a block-local variable stops existing when the block ends
- why a global variable lasts until program end
- what `new int{...}` creates in this gate
- why `delete` is required before returning from `main`

## Gate 2 Packet: Values, References, and Parameter Passing
Use this packet for:
- the difference between arguments and parameters
- pass-by-value as copying
- pass-by-reference as aliasing the caller's object
- what changes at the call site and what does not

Read in this order:

| Step | Read this | Direct link | Why this step matters | Stop when you can explain |
|------|-----------|-------------|------------------------|----------------------------|
| 1 | Introduction to function parameters and arguments | https://www.learncpp.com/cpp-tutorial/introduction-to-function-parameters-and-arguments/ | Gives the base model for what the caller passes and what the function receives | what an argument is, what a parameter is, and where each one lives |
| 2 | Pass by lvalue reference | https://www.learncpp.com/cpp-tutorial/pass-by-lvalue-reference/ | Shows how a reference parameter can let a function affect the caller's object | why changing a reference parameter can change the original variable |
| 3 | Pass by value and pass by reference | https://www.learncpp.com/cpp-tutorial/pass-by-value-and-pass-by-reference/ | Puts the two mechanisms side by side so the learner can compare outcomes | what changes after a call by value versus a call by reference |

## Scope Control for Gate 2
For `Gate 2: Values, References, and Parameter Passing`, do not continue into rvalue references, move semantics, or generic parameter-passing advice for large objects.

You are ready to stop when you can explain:
- why a by-value parameter is a separate object
- why a reference parameter is an alias in this gate
- why the same call can leave the caller unchanged in one function and changed in another

## Gate 3 Packet: First Class, Lifetime, and Basic RAII
Use this packet for:
- what a small class object is in this gate
- what constructors and destructors do
- how a small public member function can expose state to a helper function
- why scope exit gives deterministic cleanup

Read in this order:

| Step | Read this | Direct link | Why this step matters | Stop when you can explain |
|------|-----------|-------------|------------------------|----------------------------|
| 1 | Introduction to classes | https://www.learncpp.com/cpp-tutorial/introduction-to-classes/ | Gives the basic model for a user-defined type, its public interface, and how outside code can use an object | what the class is in this gate, what each object instance represents, and how `observe` can read the tag through the class interface |
| 2 | Introduction to constructors | https://www.learncpp.com/cpp-tutorial/introduction-to-constructors/ | Shows what runs automatically when an object is created | why object setup belongs in the constructor for this gate |
| 3 | Introduction to destructors | https://www.learncpp.com/cpp-tutorial/introduction-to-destructors/ | Shows what runs automatically when an object is destroyed and introduces the cleanup idea behind RAII | why the destructor runs at scope exit and why that makes cleanup deterministic |

## Scope Control for Gate 3
For `Gate 3: First Class, Lifetime, and Basic RAII`, do not continue yet into copy constructors, move semantics, inheritance, or smart pointers.

You are ready to stop when you can explain:
- what object is created by each class definition line in the task
- how the helper function can read the tag through a small public member function
- why the constructor runs when the object is created
- why the destructor runs when the object's lifetime ends
- why tying cleanup to the destructor is the basic RAII idea in this gate

## Gate 4 Packet: Phase 0 Integration Project
Use this packet as a refresh map before the phase-ending integration gate.

Read only the weak part you need:

| Weak area | Read this | Direct link | Stop when you can explain |
|-----------|-----------|-------------|----------------------------|
| compile command, warnings, or basic console I/O | Configuring your compiler: Warning and error levels | https://www.learncpp.com/cpp-tutorial/configuring-your-compiler-warning-and-error-levels/ | why the integration gate still requires the baseline command and zero warnings |
| reading and printing values in the report | Introduction to iostream: cout, cin, and endl | https://www.learncpp.com/cpp-tutorial/introduction-to-iostream-cout-cin-and-endl/ | how the input values enter the program and how the report is printed |
| dynamic integer plus cleanup | Dynamic memory allocation with new and delete | https://www.learncpp.com/cpp-tutorial/dynamic-memory-allocation-with-new-and-delete/ | why the heap value still needs a matching `delete` and pointer reset in the integration task |
| preview vs apply behavior | Pass by value and pass by reference | https://www.learncpp.com/cpp-tutorial/pass-by-value-and-pass-by-reference/ | why one helper changes only a copy while the other changes the caller's variable |
| scope logging and deterministic cleanup | Introduction to destructors | https://www.learncpp.com/cpp-tutorial/introduction-to-destructors/ | why the inner object leaves before the outer object in the integration task |

## Scope Control for Gate 4
For `Gate 4: Phase 0 Integration Project`, use this packet as targeted refresh only.

Do not turn the refresh into a new full reading sprint if only one concept is weak.

You are ready to stop when you can sketch:
- where the program's input comes from
- which variable is global, local, and dynamic
- which helper is by value and which is by reference
- why the inner scope object leaves before the outer one

## Phase 1 Packet A: Numeric Safety Fundamentals
Use this packet for:
- signed and unsigned integer ranges and overflow behavior
- floating-point representation and precision limits
- numeric conversions and arithmetic conversions
- why direct floating-point equality is fragile

Read in this order:

| Step | Read this | Direct link | Why this step matters | Stop when you can explain |
|------|-----------|-------------|------------------------|----------------------------|
| 1 | Signed integers | https://www.learncpp.com/cpp-tutorial/signed-integers/ | Gives the basic range and overflow model for ordinary integers | what signed overflow means and why integer width matters |
| 2 | Unsigned integers, and why to avoid them | https://www.learncpp.com/cpp-tutorial/unsigned-integers-and-why-to-avoid-them/ | Teaches why unsigned arithmetic often creates surprises instead of safety | when unsigned values help and why they often harm beginner reasoning |
| 3 | Floating point numbers | https://www.learncpp.com/cpp-tutorial/floating-point-numbers/ | Builds the mental model for precision, rounding, and representation limits | why `double` is usually preferred and why precision loss exists |
| 4 | Narrowing conversions, list initialization, and constexpr initializers | https://www.learncpp.com/cpp-tutorial/narrowing-conversions-list-initialization-and-constexpr-initializers/ | Connects type ranges to narrowing and gives a first safe-default rule through brace initialization | when converting between numeric types changes the value or meaning and why brace initialization rejects some bad conversions |
| 5 | Arithmetic conversions | https://www.learncpp.com/cpp-tutorial/arithmetic-conversions/ | Explains how mixed-type arithmetic picks a common type | what happens when integers and floating-point values meet in one expression |
| 6 | Relational operators and floating point comparisons | https://www.learncpp.com/cpp-tutorial/relational-operators-and-floating-point-comparisons/ | Gives the first workable rule for comparing calculated floating-point values | why `==` on calculated floating-point values is usually unsafe |

## Scope Control for Phase 1 Packet A
For `Phase 1: Numeric Safety Fundamentals`, do not continue into bitwise operators, numeral-systems deep dives, or optimization-heavy numeric topics unless a later gate explicitly asks for them.

Treat step 6 as required only if floating-point comparison is part of the assigned scope; otherwise it is a follow-up clarification.

You are ready to stop when you can explain:
- the difference between signed and unsigned integer behavior
- why overflow risk is part of type choice
- why floating-point values have precision limits
- how an expression can change type through implicit conversions
- why comparing calculated floating-point values for exact equality is risky

## Phase 1 Packet B: Aggregates and Language Organization
Use this packet for:
- what a program-defined type is
- unscoped and scoped enums as bounded symbolic sets
- simple structs and plain-data aggregates as grouped data
- namespaces as the first step toward cleaner program organization

Read in this order:

| Step | Read this | Direct link | Why this step matters | Stop when you can explain |
|------|-----------|-------------|------------------------|----------------------------|
| 1 | Introduction to program-defined (user-defined) types | https://www.learncpp.com/cpp-tutorial/introduction-to-program-defined-user-defined-types/ | Gives the mental model for types the program defines itself | why enums and structs need definitions before use |
| 2 | Introduction to structs, members, and member selection | https://www.learncpp.com/cpp-tutorial/introduction-to-structs-members-and-member-selection/ | Introduces simple grouped data without full class-surface complexity | how a struct groups related state and how member access works |
| 3 | Struct aggregate initialization | https://www.learncpp.com/cpp-tutorial/struct-aggregate-initialization/ | Adds the first safe brace-initialized model for plain-data structs | what an aggregate is at this level and how memberwise initialization works |
| 4 | Unscoped enumerations | https://www.learncpp.com/cpp-tutorial/unscoped-enumerations/ | Shows how to model a bounded set of named values | when an enum is better than a loose integer or magic number |
| 5 | Scoped enumerations (enum classes) | https://www.learncpp.com/cpp-tutorial/scoped-enumerations-enum-classes/ | Adds the safer scoped form once the basic enum idea is stable | why `enum class` reduces accidental conversions and name collisions |
| 6 | Naming collisions and an introduction to namespaces | https://www.learncpp.com/cpp-tutorial/naming-collisions-and-an-introduction-to-namespaces/ | Gives the problem statement that namespaces solve | why the global namespace becomes fragile as a program grows |
| 7 | User-defined namespaces and the scope resolution operator | https://www.learncpp.com/cpp-tutorial/user-defined-namespaces-and-the-scope-resolution-operator/ | Adds the first practical tool for avoiding naming collisions and organizing code | why `Name::thing` exists and when a namespace boundary helps |

## Scope Control for Phase 1 Packet B
For `Phase 1: Aggregates and Language Organization`, do not continue into unions, header/linkage rules, argument-dependent lookup, or broad multi-file design topics unless the gate explicitly asks for them.

You are ready to stop when you can explain:
- what makes a type program-defined
- when an enum or `enum class` is a good fit for bounded symbolic values
- when a small struct or plain-data aggregate is a good fit for grouped data
- how a namespace changes naming and lookup at a first-pass level

## Phase 1 Packet C: Function Surface and Preprocessor Minimum
Use this packet for:
- overloading and overload resolution
- default arguments and inline as first interface-surface tools
- simple recursion
- the minimum preprocessor model needed for headers and includes

Read in this order:

| Step | Read this | Direct link | Why this step matters | Stop when you can explain |
|------|-----------|-------------|------------------------|----------------------------|
| 1 | Introduction to function overloading | https://www.learncpp.com/cpp-tutorial/introduction-to-function-overloading/ | Gives the first model for multiple functions sharing a name | what makes two overloads distinct |
| 2 | Function overload resolution and ambiguous matches | https://www.learncpp.com/cpp-tutorial/function-overload-resolution-and-ambiguous-matches/ | Turns overload choice into something the learner can reason about instead of guess | what makes a call ambiguous and how the language picks a best match |
| 3 | Default arguments | https://www.learncpp.com/cpp-tutorial/default-arguments/ | Adds a bounded way to simplify call sites without multiplying overloads | what a default argument is and where defaults belong |
| 4 | Inline functions and variables | https://www.learncpp.com/cpp-tutorial/inline-functions-and-variables/ | Teaches the modern ODR and header meaning of `inline` | why `inline` is mainly about multiple definitions, not guaranteed speed |
| 5 | Recursion | https://www.learncpp.com/cpp-tutorial/recursion/ | Adds the first self-calling control-flow model | what a base case does and why missing it breaks recursion |
| 6 | Introduction to the preprocessor | https://www.learncpp.com/cpp-tutorial/introduction-to-the-preprocessor/ | Gives the first mental model for text transformation before compilation | what preprocessing does before the compiler sees a translation unit |
| 7 | Header files | https://www.learncpp.com/cpp-tutorial/header-files/ | Connects function declarations and shared interfaces to multi-file structure | why headers exist and what belongs in them at a first-pass level |
| 8 | Header guards | https://www.learncpp.com/cpp-tutorial/header-guards/ | Gives the minimum safe rule for repeated inclusion | what a header guard prevents |

## Scope Control for Phase 1 Packet C
For `Phase 1: Function Surface and Preprocessor Minimum`, do not continue into lambda lessons through LearnCpp until `R-006` has been assigned, and do not continue into macro programming, conditional-compilation policy, or template-heavy callable abstractions unless a later gate explicitly asks for them.

You are ready to stop when you can explain:
- what makes an overload set ambiguous or clear
- what a default argument does at the call site
- why `inline` matters for header definitions more than for performance folklore
- what a base case is in simple recursion
- what the preprocessor does at a first-pass level and why header guards matter

## Phase 1 Packet D: Special Members and Dynamic Object Model
Use this packet for:
- copy constructor and memberwise copy basics
- copy elision, shallow versus deep copying, and the hidden `this` pointer
- first-pass inheritance and virtual dispatch
- restrained operator overloading and the first callable-object intuition

Read in this order:

| Step | Read this | Direct link | Why this step matters | Stop when you can explain |
|------|-----------|-------------|------------------------|----------------------------|
| 1 | Introduction to the copy constructor | https://www.learncpp.com/cpp-tutorial/introduction-to-the-copy-constructor/ | Gives the first real model for what happens when one class object is initialized from another | when copying a class object calls a copy constructor and what memberwise copy means |
| 2 | Class initialization and copy elision | https://www.learncpp.com/cpp-tutorial/class-initialization-and-copy-elision/ | Prevents the learner from depending on copy-constructor side effects | why a copy may be elided and why that matters for reasoning |
| 3 | Shallow vs deep copying | https://www.learncpp.com/cpp-tutorial/shallow-vs-deep-copying/ | Establishes the safety boundary around resource ownership and copying | why a shallow copy is dangerous for an owning raw pointer |
| 4 | The hidden `this` pointer and member function chaining | https://www.learncpp.com/cpp-tutorial/the-hidden-this-pointer-and-member-function-chaining/ | Makes member calls and object-as-implicit-parameter reasoning concrete | what `this` points to and how member chaining works |
| 5 | Introduction to inheritance | https://www.learncpp.com/cpp-tutorial/introduction-to-inheritance/ | Introduces base/derived vocabulary and the idea of extending a type's interface and behavior | what an `is-a` relationship means and what inheritance is trying to model |
| 6 | Basic inheritance in C++ | https://www.learncpp.com/cpp-tutorial/basic-inheritance-in-c/ | Gives the first practical mechanics of accessing the base and derived surfaces | what a derived object contains relative to its base |
| 7 | Virtual functions | https://www.learncpp.com/cpp-tutorial/virtual-functions/ | Adds the first dynamic-dispatch mental model | why a call through a base reference or pointer can reach a derived override |
| 8 | Virtual destructors, virtual assignment, and overriding virtualization | https://www.learncpp.com/cpp-tutorial/virtual-destructors-virtual-assignment-and-overriding-virtualization/ | Introduces the first safety rule for polymorphic bases | why a polymorphic base needs a virtual destructor |
| 9 | Introduction to operator overloading | https://www.learncpp.com/cpp-tutorial/introduction-to-operator-overloading/ | Gives the rule that operators are just functions with semantic expectations | when operator overloading is appropriate and when it becomes misleading |
| 10 | Overloading the parenthesis operator | https://www.learncpp.com/cpp-tutorial/overloading-the-parenthesis-operator/ | Gives the first callable-object bridge without forcing STL algorithms first | what `operator()` enables and why a functor is still an object |

## Scope Control for Phase 1 Packet D
For `Phase 1: Special Members and Dynamic Object Model`, do not continue into move semantics, smart pointers, RTTI, object layout, multiple inheritance, or raw-owning deep-copy tasks unless a later gate explicitly asks for them.

Keep the packet in Rule-of-Zero and value-semantics territory by default. Prefer examples that compose with standard library value types instead of raw ownership.

You are ready to stop when you can explain:
- what memberwise copy means and why copy elision changes what you can observe
- why shallow copying becomes dangerous for owning resources
- what `this` points to in a member function
- what inheritance and virtual dispatch buy, and what their first costs are
- when a small operator overload or `operator()` use is justified
